# Task: Add a basic CLI entrypoint for running Evolvo
## Acceptance criteria
- `app/main.py` exists and exposes a `main()` function.
- Running `python -m app.main` executes without syntax/runtime errors.
- Behavior remains minimal and backward compatible with existing project structure.

## Reflection
- Verified `app/main.py` already exists and exposes `main()` with a `if __name__ == "__main__": main()` entrypoint.
- Executed `python -m app.main`; command completed successfully with no syntax/runtime errors.
- No code changes were required because acceptance criteria were already satisfied.
